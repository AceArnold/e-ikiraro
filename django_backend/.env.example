# ============================================================================
# E-ikiraro Environment Variables Template
# ============================================================================
# IMPORTANT: Copy this file to .env and fill in your own values.
# DO NOT commit .env to version control - it contains sensitive information!
# If a secret is accidentally committed, rotate it immediately.
# ============================================================================

# ============================================================================
# DJANGO CORE SETTINGS
# ============================================================================

# Django secret key for cryptographic operations (REQUIRED)
# Generate a secure key with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Then paste the output here
SECRET_KEY=

# Debug mode (REQUIRED)
# Set to True for development, False for production
DEBUG=True

# ============================================================================
# EMAIL CONFIGURATION (REQUIRED for email verification and notifications)
# ============================================================================
# The app sends:
# - Email verification links during registration
# - Password reset emails
# - Application confirmation emails
# - Notifications

# Email backend type (usually don't change this)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP server address
# Examples:
#   Gmail: smtp.gmail.com
#   Outlook: smtp-mail.outlook.com
#   Yahoo: smtp.mail.yahoo.com
#   Custom: your-mail-server.com
EMAIL_HOST=smtp.gmail.com

# SMTP port
# Usually 587 (TLS) or 465 (SSL)
EMAIL_PORT=587

# Email account username (usually your email address)
EMAIL_HOST_USER=your-email@gmail.com

# Email account password
# IMPORTANT: For Gmail, generate an App Password:
#   1. Go to https://myaccount.google.com/
#   2. Select Security from the left menu
#   3. Enable 2-Step Verification if not already enabled
#   4. Create an app password for "Mail" and "Windows Computer"
#   5. Use the 16-character password here
# For other providers, use your account password
EMAIL_HOST_PASSWORD=

# Enable TLS encryption for email (recommended)
EMAIL_USE_TLS=True

# Default email address to send from
DEFAULT_FROM_EMAIL=your-email@gmail.com

# ============================================================================
# GOOGLE OAUTH 2.0 CONFIGURATION (OPTIONAL)
# ============================================================================
# Enable "Sign in with Google" feature
# See README.md for detailed setup instructions at:
# https://github.com/AceArnold/e-ikiraro#google-oauth-login-setup
#
# Summary:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add redirect URI: http://127.0.0.1:8000/accounts/google/login/callback/
# 6. Copy Client ID and Client Secret below

# Google OAuth Client ID (format: xxxxx.apps.googleusercontent.com)
# Leave blank to disable Google OAuth - email/password login still works fine
GOOGLE_CLIENT_ID=

# Google OAuth Client Secret
# KEEP THIS PRIVATE! Never share or commit this value.
# If exposed, delete the OAuth app and create a new one immediately.
GOOGLE_CLIENT_SECRET=

# ============================================================================
# OPTIONAL: DEVELOPMENT SETTINGS
# ============================================================================

# Alternative: Use console email backend during development
# This prints emails to console instead of sending them
# Uncomment the line below to enable:
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# ============================================================================
# REFERENCE: ENVIRONMENT VARIABLES CHECKLIST
# ============================================================================
# Required (app won't run without these):
#   ✓ SECRET_KEY
#   ✓ DEBUG
#   ✓ EMAIL_BACKEND, EMAIL_HOST, EMAIL_PORT, EMAIL_HOST_USER, EMAIL_HOST_PASSWORD, EMAIL_USE_TLS, DEFAULT_FROM_EMAIL
#
# Optional (features will be disabled if not set):
#   ✓ GOOGLE_CLIENT_ID & GOOGLE_CLIENT_SECRET (enables Google login)
#
# Testing:
#   - Email/password login works without any email configuration
#   - All application features work without Google OAuth
#   - Use console email backend for development
#
# ============================================================================